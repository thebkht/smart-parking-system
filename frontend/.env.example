# API base URL for the SmartParking backend.
# Default: http://localhost:8000 (works for local dev with browser on the same machine).
# For a phone/tablet on the same Wi-Fi, use the host machine's LAN IP:
#   VITE_API_BASE=http://192.168.1.100:8000
VITE_API_BASE=http://localhost:8000
